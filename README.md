Environments for basic edition yii2

Install
-------
`composer require carono/yii2-basic-env`

exec `yii env` - creating environments folder and script in root application  
exec `init` - set environment
